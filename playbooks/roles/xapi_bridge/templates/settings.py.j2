# generated by Ansible. Do not edit

# the maximum age of an event, in seconds, before it is published to the LRS
PUBLISH_MAX_WAIT_TIME = {{xapi_PUBLISH_MAX_WAIT_TIME }}

# the number of statements to publish per batch
PUBLISH_MAX_PAYLOAD = {{xapi_PUBLISH_MAX_PAYLOAD}}

# maximum publish retries on LRS connection error
PUBLISH_MAX_RETRIES = {{xapi_PUBLISH_MAX_RETRIES}}

# sleep time between polls of watched log file, in seconds
NOTIFIER_READ_FREQ = {{xapi_NOTIFIER_READ_FREQ}}

# how long to wait for activity when polling, in milliseconds
# https://docs.python.org/2.4/lib/poll-objects.html
# If timeout is omitted, negative, or None, the call will block until
# there is an event for this poll object.
NOTIFIER_POLL_TIMEOUT = {{xapi_NOTIFIER_POLL_TIMEOUT}}

# lrs credentials
LRS_ENDPOINT = "{{xapi_LRS_ENDPOINT}}"
LRS_USERNAME = "{{xapi_LRS_USERNAME}}"
LRS_PASSWORD = "{{xapi_LRS_PASSWORD}}"
LRS_BASICAUTH_HASH = "{{xapi_LRS_BASICAUTH_HASH}}"

LRS_BACKEND_TYPE = "{{xapi_LRS_BACKEND_TYPE}}"

OPENEDX_PLATFORM_URI = "{{xapi_OPENEDX_PLATFORM_URI}}"
OPENEDX_USER_API_URI = OPENEDX_PLATFORM_URI + "/api/user/v1/"
OPENEDX_OAUTH2_CLIENT_ID = "{{xapi_OPENEDX_OAUTH2_CLIENT_ID}}"
OPENEDX_OAUTH2_CLIENT_SECRET = "{{xapi_OPENEDX_OAUTH2_CLIENT_SECRET}}"

LMS_API_USE_MEMCACHED = {{xapi_LMS_API_USE_MEMCACHED}}
MEMCACHED_ADDRESS = "{{xapi_MEMCACHED_ADDRESS}}"

# events configuration
# list of ignored event ids
IGNORED_EVENT_TYPES = {{xapi_IGNORED_EVENT_TYPES}}

# Debugging
EXCEPTIONS_NO_CONTINUE = {{xapi_EXCEPTIONS_NO_CONTINUE}}  # set to True to always raise, fail application

# Sentry.io integration
SENTRY_DSN = "{{xapi_SENTRY_DSN}}"
if SENTRY_DSN:
    import sentry_sdk
    sentry_sdk.init(SENTRY_DSN)

# open a TCP socket and HTTP server for simple OK status response
HTTP_PUBLISH_STATUS = {{xapi_HTTP_PUBLISH_STATUS}}
HTTP_PUBLISH_IP = "{{xapi_HTTP_PUBLISH_IP}}"
HTTP_PUBLISH_PORT = {{xapi_HTTP_PUBLISH_PORT}}

# Debugging
DEBUG_MODE = {{xapi_DEBUG_MODE}}
EXCEPTIONS_NO_CONTINUE = {{xapi_EXCEPTIONS_NO_CONTINUE}}  # set to True to always raise, fail application

# Testing
TEST_LOAD_TRACKING_TOTAL_LOG_WRITES = {{xapi_TEST_LOAD_TRACKING_TOTAL_LOG_WRITES}}
TEST_LOAD_SLEEP_SECS_BETWEEN_WRITES = {{xapi_TEST_LOAD_SLEEP_SECS_BETWEEN_WRITES}}
TEST_LOAD_TRACKING_FILE_SOURCE = "{{xapi_TEST_LOAD_TRACKING_FILE_SOURCE}}"
TEST_LOAD_SUCCESSFUL_STATEMENTS_BENCHMARK = {{xapi_TEST_LOAD_SUCCESSFUL_STATEMENTS_BENCHMARK}}
